spring.profiles.active=dev

# 热部署 生效
spring.devtools.restart.enabled=true
# 监听的目录，这个目录下的类会 重新加载
#spring.devtools.restart.additional-paths=src/main/java
# src/ 忽略了，每次修改代码不会重启，需要 build 一下 生产新的 class
spring.devtools.restart.exclude=templates/**,static/**,src/** 

